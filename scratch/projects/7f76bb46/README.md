# Comments Management Platform
> Generated by **KirstonAI Coding Agent**

## Requirement
> "Add authentication and comment moderation endpoints"

## Setup
```bash
cd backend && pip install -r requirements.txt && uvicorn app.main:app --reload
cd frontend && npm install && npm run dev
```
